<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fila.app.mapper.cart.WishAddMapper">

  <!-- 중복 체크 (SELECT문 그대로) -->
  <select id="exists" resultType="boolean">
    SELECT 1 FROM WISHLIST WHERE USER_NUMBER = #{userNumber} AND PRODUCT_ID = #{productId}
  </select>

  <!-- INSERT (INSERT문 그대로) -->
  <insert id="insert">
    INSERT INTO WISHLIST (WISHLIST_ID, USER_NUMBER, PRODUCT_ID, CREATED_AT)
    VALUES (SEQ_WISHLIST.NEXTVAL, #{userNumber}, #{productId}, SYSDATE)
  </insert>

</mapper>