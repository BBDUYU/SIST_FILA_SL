<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fila.app.mapper.member.MemberMapper">

    <select id="login" resultType="com.fila.app.domain.member.MemberVO">
        SELECT
            user_number     AS userNumber,
            id,
            password,
            name,
            email,
            phone,
            birthday,
            gender,
            status,
            role,
            grade,
            created_at      AS createdAt,
            updated_at      AS updatedAt
        FROM users
        WHERE id = #{id}
          AND password = #{pw}
          AND status = 'ACTIVE'
    </select>

    <insert id="insert">
        INSERT INTO users (
            user_number,
            id,
            password,
            name,
            email,
            phone,
            birthday,
            gender,
            role,
            status,
            grade,
            created_at,
            updated_at
        ) VALUES (
            SEQ_USERS.NEXTVAL,
            #{id},
            #{password},
            #{name},
            #{email},
            #{phone},
            #{birthday},
            #{gender},
            #{role},
            #{status},
            #{grade},
            SYSDATE,
            SYSDATE
        )
    </insert>

    <select id="isDuplicateId" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE id = #{id}
    </select>

    <select id="existsByIdAndPhone" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE id = #{id}
          AND REPLACE(phone, '-', '') = #{phone}
    </select>

    <update id="updatePassword">
        UPDATE users
        SET password = #{pw},
            updated_at = SYSDATE
        WHERE id = #{id}
    </update>

</mapper>