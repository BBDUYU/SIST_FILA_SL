<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fila.app.mapper.member.MemberMapper">

    <!-- 🔹 아이디로 회원 조회 (로그인용, 비밀번호 비교 X) -->
    <select id="findById"
            parameterType="string"
            resultType="com.fila.app.domain.member.MemberVO">
        SELECT
            user_number     AS userNumber,
            id,
            password,
            name,
            email,
            phone,
            birthday,
            gender,
            status,
            role,
            grade
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 🔹 회원가입 -->
    <insert id="insert">
        INSERT INTO users (
            user_number,
            id,
            password,
            name,
            email,
            phone,
            birthday,
            gender,
            role,
            status,
            grade,
            created_at,
            updated_at
        ) VALUES (
            SEQ_USERS.NEXTVAL,
            #{id},
            #{password},
            #{name},
            #{email},
            #{phone},
            #{birthday},
            #{gender},
            #{role},
            #{status},
            #{grade},
            SYSDATE,
            SYSDATE
        )
    </insert>
    
   <select id="selectIdByNameAndPhone" resultType="string">
    SELECT id
    FROM users
    WHERE name = #{name}
      AND phone = #{phone}
      AND status = 'ACTIVE'
</select>

    

    <!-- 🔹 아이디 중복 체크 -->
    <select id="isDuplicateId" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 🔹 비밀번호 재설정 가능 여부 -->
    <select id="existsByIdAndPhone" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE id = #{id}
          AND REPLACE(phone, '-', '') = #{phone}
    </select>

    <!-- 🔹 비밀번호 변경 (일반) -->
    <update id="updatePassword">
        UPDATE users
        SET password = #{pw},
            updated_at = SYSDATE
        WHERE id = #{id}
    </update>

    <!-- 🔥 평문 비밀번호 회원 조회 (마이그레이션용) -->
    <select id="findPlainPasswordUsers"
            resultType="com.fila.app.domain.member.MemberVO">
        SELECT
            user_number AS userNumber,
            id,
            password
        FROM users
        WHERE password NOT LIKE '$2a$%'
    </select>

    <!-- 🔥 비밀번호 업데이트 (마이그레이션용, id 기준) -->
    <update id="updatePasswordById">
        UPDATE users
        SET password = #{pw},
            updated_at = SYSDATE
        WHERE id = #{id}
    </update>

</mapper>
